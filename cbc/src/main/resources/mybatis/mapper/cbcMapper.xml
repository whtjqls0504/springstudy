<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<!-- mapper namespace : mapper의 경로 -->
<mapper namespace="com.gdu.bbs.dao.CbcMapper">

<!-- 목록(begin/end 전달, List 반환) -->
<select id="getCbcList" parameterType="Map" resultType="CbcDto">
  SELECT   

</select>
<!-- 목록 개수(전달 없음, int 반환)-->
<select id=""></select>

<!-- 상세보기 (전달 int )-->
<select id=""></select>

<!-- 삽입(전달 CbcDto cbc, int 반환(INSERT는 resultType 작성X) -->
<insert id=""></insert>

<!-- 수정(전달 CbcDto cbc, int 반환)-->
<update id=""></update>


<!-- 삭제(전달 int cbcNo, int 반환)-->
<delete id=""></delete>


</mapper>