package com.gdu.cbc.dao;

import java.util.List;
import java.util.Map;

import org.apache.ibatis.annotations.Mapper;

import com.gdu.cbc.dto.CbcDto;

@Mapper
public interface CbcMapper {
  
  // 목록(end/begin)을 전달한다.
  // 이 친구가 부르는거 -> xml, (resource에 저장됨)
  
}
